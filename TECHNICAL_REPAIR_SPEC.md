# 2026 华数杯 C 题技术修复规格

状态：执行中  
建立日期：2026-08-08

## 范围

本规格只处理当前 C 题的预测、任务调度、储能调度、联合多目标优化及其证据链。
旧的 model v41、code v14、solve v11、paper v17、review v11 保持不可变。

## 版本与目录

- 新实现只能写入新的 checkpoint 版本；预计从 model v42、code v15、solve v12、
  paper v18、review v12 开始。
- 新导出写入 `output/technical-repair-<run-id>/`，不得覆盖旧输出证据。
- 两次独立复算放在题目父目录的 `C题技术验证/run-1` 与 `run-2`。
  两个目录只保存不可变输入副本和本轮新输出，任务结束后作为验证证据保留；
  清理必须另行获得删除授权。
- 一次性生成脚本或诊断文件放在新 checkpoint 或上述验证目录，不散落到题目根目录。

## 运行合同

- 正式程序默认无墙钟截止。
- 候选数、最大迭代数、收敛阈值和连续无改进轮数必须预先声明。
- 仅显式设置 `MMW_MAX_RUNTIME_SECONDS` 时启用保护性墙钟；触发后写
  `external_timeout`、`incomplete=true`，不得审批或使用部分结果。
- 结果等级保持 `scenario-feasible`；没有 evaluator-only Oracle，不得标为
  `verified`。

## Q1 合同

- 训练区间 0--2351，验证区间 2352--2375，测试区间 2376--2399。
- 验证与测试均采用严格递归 24 步预测，测试标签不得进入特征。
- 每个区域/任务类型序列比较 SeasonalNaive24、SeasonalNaive168 与预声明 Huber
  网格；验证集按 RMSE、MAE、WAPE、固定模型顺序确定性选型。
- 用 0--2375 重拟合后预测测试集，并输出每序列样本数、零分母标记、模型及参数。

## Q2 合同

- 原容量/时延贪心方案作为公开基线。
- 正式输出 cost_min、carbon_min、renewable_max、service_first、balanced 五个命名
  方案；最终方案必须完成 50,000 任务全量审计。
- 候选评价使用区域-小时资源数组的增量更新，不得每个候选复制完整调度表。
- small/medium/large 快速候选上限固定为 2,000/10,000/50,000；正式使用 large。
- 输出访问任务、候选类型、可行/拒绝/接受数量、停止原因、近优拒绝候选及成本分解。

## Q3 合同

- no-action 必须进入正式候选集。
- 至少比较成本、碳、可再生能源、峰值、爬坡与平衡方案；重复轨迹不得重复计数。
- 逐区域满足 SOC 转移、功率、效率、购售电和互斥约束；基准终端条件为
  `SOC_T = SOC_0`，放宽条件仅进入敏感性分析。
- 平衡方案必须经过 Pareto 支配过滤；若其他方案均被 no-action 支配，则选择
  no-action。

## Q4 合同

- 每轮先固定任务求储能，再根据本轮逐时边际信号重新生成任务邻域。
- 最大 8 轮，连续 2 轮无改进停止，改善容差固定为 `1e-8`。
- 每次接受任务更新后执行 50,000 任务全量审计。
- 随机场景固定种子 2026、2027、2028；每个场景独立重启交替优化。
- 输出即时任务 SLA、按期完成率、最大等待、等待分位数、网络时延、迁移数以及逐轮
  候选和 incumbent 哈希。

## 验收

1. Q1 防泄漏测试通过，18 条序列均有独立验证选型证据。
2. Q2 五个正式方案全部有全任务审计和五类目标指标。
3. Q3 no-action 被正式比较，候选轨迹可区分，最终方案非支配。
4. Q4 trace 证明每轮根据上一轮结果重新生成候选。
5. 两个隔离目录独立运行完成，均无外部超时且硬约束残差不超过 `1e-6`。
6. 方法合同、结果、论文和 review 绑定同一新版本，不声称全局最优。

## 最终独立运行目录

- `C题技术验证/run-1-final` 与 `run-2-final` 使用修正后的同一冻结脚本独立复算。
- 早期 `run-1`、`run-2` 和 `development/attempt-1-partial` 仅作为尝试记录保留，不作为最终结果证据。


- `run-1-final-fast` 与 `run-2-final-fast` 为最终验收运行；其完整审计与参考实现逐列等价，前一组 final 目录因性能根因修正而保留为中止记录。

