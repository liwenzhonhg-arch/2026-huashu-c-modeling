# v42 Verifier 独立核对

结论：warning，可进入 code。

- Q1 的训练、验证、测试边界和递归特征语义明确，测试标签不进入预测特征。
- Q2 五类方案共享明确的50000候选合同，每个正式方案要求50000任务全量审计。
- Q3 将no-action纳入候选，终端SOC等于初始SOC，互斥和Pareto选择可确定性复核。
- Q4 每轮依据上一轮储能信号重建邻域，具有8轮/2轮无改进/1e-8确定性停止。
- 默认无墙钟；显式外部超时必须incomplete并阻断。
- 局限：离散SOC和有限候选仅支持best-found、scenario-feasible，不支持全局最优。
